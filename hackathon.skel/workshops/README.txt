workshops each have a subdirectory here.  accounts created for that workshop
appear as files in that subdirectory whose name is the account name (like
"pmemuser1") and the file contents are the name collected duroing account
creation (used only to help find someone's account if they forget it).
